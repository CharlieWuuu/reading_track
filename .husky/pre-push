# 只跑 lint + format + types（約 16 秒）。測試在 CI 上跑——
# 三分鐘的 pre-push 會讓人養成 --no-verify 的習慣，那比沒有 hook 更糟
npm run check:fast

# schema 改了卻沒有對應的 migration，推上去資料庫就跟程式碼對不上。
# db:generate 是唯一還要人記得的一步（CI 產不出來、也沒地方 commit），
# 所以這裡提醒——「人要記得」就是 0016、0017 兩次事故的共同根因
#
# 不檢查「正式庫跑到哪」：migration 現在由部署前的 db-ci 跑（見 package.json
# 的 build），所以推之前本來就會落後一支。那個檢查會擋住每一次帶 migration
# 的推送，逼人手動先跑一遍——等於把剛自動化掉的步驟又還給人
npm run db:drift
