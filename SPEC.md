# Archivum 規格

> 這份檔案由 `_model.py` 產生（設計稿的產生器），不要手改。
> 模型改了就重跑，設計稿與這份規格會一起更新。
> 設計稿：https://claude.ai/code/artifact/bd10df68-9cf6-424f-a60f-561ae11e4f18

## 三種東西

資料只有三堆，其餘都是它們的「類型」。類型是資料不是程式——新增類型＝勾模組，不改 code。

### 紀錄（輸入）

外面的東西經過我。我登記它，記的是「我接觸了什麼」。**同一張表單**，換類型只是換幾個欄位。

- 類型：書籍、文章、電影、Podcast、YouTube
- 表單：共用一張表單與一套清單／詳情。**抓取器不共用**——貼書店網址、貼 YouTube 連結是兩支剖析器，但填進去的欄位是同一組。
- 註：新增類型＝勾模組＋（可選）寫一支抓取器。

### 片段（從紀錄上掉下來的）

讀到一半掉出來的東西。不是完整的紀錄，也還不是我的輸出——是**把破碎知識接起來的接點**。

- 類型：佳句、單字、關鍵字
- 表單：極短的表單：一句話＋出處＋位置。出處可以留空（在路上聽到的也算）。不強迫寫感想——**挑選本身就是想法**。
- 註：關鍵字也在這一堆：它摘的是概念，跟佳句摘句子、單字摘詞是同一件事。

### 專欄（輸出）

我自己產生的字。長短不拘，從「今天睡好少」到一篇論述都算。差別只在**類型**與有沒有掛出處。

- 類型：日記、心得、論述、每日計畫
- 表單：共用一張表單：長文＋日期＋類型＋關鍵字。掛了出處就是心得，沒掛就是日記——不用先做決定。
- 註：分類往下展開就對了，不用再拆表。

## 類型 × 模組

● 必備　○ 選用　— 沒有

| 模組 | 說明 | 書籍 | 文章 | 電影 | 佳句 | 單字 | 關鍵字 | 日記 | 心得 | （新類型） |
|---|---|---|---|---|---|---|---|---|---|---|
| 標題 | 一行字，清單上顯示的那個 | ● | ● | ● | — | ● | ● | ○ | ● | ？ |
| 作者／來源人 | 誰講的、誰寫的 | ● | ● | ● | ○ | — | — | — | — | ？ |
| 長文 | 多段落，支援分欄 | — | — | — | — | — | ○ | ● | ● | ？ |
| 一句話 | 單段，不折行的短內容 | — | — | — | ● | ● | — | — | — | ？ |
| 解釋 | 對這個東西本身的說明 | — | — | — | — | ● | ● | — | — | ？ |
| 出處 | 指向另一筆條目 | — | — | — | ● | ○ | — | — | ● | ？ |
| 位置 | 出處裡的頁碼或時間點 | — | — | — | ● | ○ | — | — | — | ？ |
| 封面圖 | 清單上的縮圖 | ● | — | ● | — | — | — | — | — | ？ |
| 外部連結 | 原始頁面，要有外開圖示 | ● | ● | ● | — | — | — | — | — | ？ |
| 進度與狀態 | 想／在／完，含日期區間 | ● | ● | ● | — | — | — | — | — | ？ |
| 單一日期 | 發生在哪一天 | — | ● | ● | ● | ● | — | ● | ● | ？ |
| 關鍵字 | 多對多，指向另一個片段 | ● | ● | ● | ● | ● | — | ● | ● | ？ |
| 量＋單位 | 頁／字／分鐘，統計讀這個 | ● | ● | ● | — | — | — | ● | ● | ？ |
| 私人 | 鎖起來，別人看不出存在 | ● | ● | ● | ● | ● | ● | ● | ● | ● |

新模組的門檻：**至少三個類型會用到**。只有一個類型用得到的，那是那個類型的欄位，不是模組。

## 量與單位

「量」是模組，單位跟著類型走。統計讀「量＋單位」自己長句子，加新類型不用改統計程式。

| 類型 | 單位 | 統計長出來的句子 |
|---|---|---|
| 書籍 | 頁 | 今年 12,480 頁 |
| 文章 | 字 | 今年 32 萬字 |
| 電影 | 分鐘 | 今年 60 小時 |
| Podcast | 分鐘 | 今年 41 小時 |
| 日記 | 字 | 今年 8.7 萬字 |

## 導覽

```
報頭（全寬）  第 N 天 · 日期 ─── Archivum ─── 上次紀錄 · ⌘K
側欄          紀錄　書籍 文章 電影
              片段　佳句 單字 關鍵字
              專欄　日記 心得 論述
              ───
              統計 · 設定 · charlie      ← 底部，實線隔開
內容          頁名 數量 · 狀態切分 ─── 概覽 列表 ─── 篩選 ＋新增
```

- 側欄只列**有資料的類型**，沒用過的不佔位置
- 統計與設定在底部工具區，不跟內容類型並列（統計是副產品，不是目的）
- 搜尋不常駐，收成 `⌘K`

## 檢視

| 檢視 | 用途 | 備註 |
|---|---|---|
| 概覽 | 預設。頭條＋月份連續流＋右側窄欄 | 條目依內容豐富度分三層 |
| 列表 | 一列一筆、欄位對齊，掃描與比較 | 即原本的「表格」 |
| 編輯 | 列表上的開關：就地編輯、多選、Tab 跳欄 | **不是第三種檢視** |

卡片檢視已砍。封面牆改放統計頁的「今年的書」——它是回顧，不是瀏覽方式。

## 數字放哪

- **形狀**：一行字說得完 → 頁內；需要畫圖才看得懂 → 統計頁
- **維度**：對這一頁的資料切一刀 → 頁內；切兩刀以上 → 統計頁
- **跨類型**：一律統計頁（在任何類型頁裡都是別人的數字）
- 詳情頁那四個數字（佳句／單字／專欄／關鍵字）是**目錄**不是統計——指向內容的是目錄，指向理解的才是統計

三條實作規則：

1. 統計頁的每個數字都要能點回清單（篩選後的清單）
2. 同一個數字只算一次，頁內與統計頁讀同一支查詢；不一致就是 bug
3. 頁內數字不點擊、不展開——它是狀態列不是控制項

## 私人項目

鎖起來的東西**看不出存在**：不放鎖頭、不留佔位、不寫「N 筆已隱藏」，數量統計要過濾後才算
（本人看 312 本／別人看 309 本），搜尋也搜不到。過濾在伺服器端，不是畫面層。

## 視覺語彙

- 報頭雙線 3px + 1.4px；區段小標籤配 1–2px 實線；細線分隔不用卡片框
- 襯線值（Noto Serif TC）配無襯線標籤（Noto Sans TC）
- 右側窄欄放固定長度的清單；狀態靠一顆點的深淺，不用底色
- 字級：站名 40 → 頁名 30 → 頭條 34 → 條目 17／15／13.5 → meta 11
- 一頁只有一個主角，主角至少是次要元素的三倍
- 配色取自靜野封面：紙白 #F7F5F0、墨 #1C1B18、仙人掌綠 #4F6B4A、沙丘 #C08A4E、天空 #8FA3AE
  （沙與天空只做圖表與色塊，不拿來寫字——對比不足 4.5:1）
